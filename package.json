{
  "name": "open-all-links",
  "version": "1.0.0",
  "description": "Chrome extension that opens all links on the current page in new tabs.",
  "main": "popup.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint --ext .js .",
    "build": "mkdir -p build && cp -r manifest.json popup.html popup.js icons icon.svg src build/ && cd build && zip -r ../open-all-links.zip *"
  },
  "keywords": [
    "chrome-extension",
    "links",
    "tabs"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
